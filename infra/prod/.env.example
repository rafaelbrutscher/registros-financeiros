# ── Aplicação ──────────────────────────────────────────────────────────────────
APP_KEY=base64:CHANGE_ME_execute_php_artisan_key_generate
APP_ENV=production
APP_URL=http://177.44.248.118:8091
APP_DEBUG=false

# ── Banco de dados ─────────────────────────────────────────────────────────────
DB_CONNECTION=mysql
DB_HOST=mariadb
DB_PORT=3306
DB_DATABASE=registros_financeiros_prod
DB_USERNAME=laravel
DB_PASSWORD=CHANGE_ME
DB_ROOT_PASSWORD=CHANGE_ME_ROOT

# ── E-mail (Mailpit compartilhado via shared_net) ──────────────────────────────
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_FROM_ADDRESS=noreply@prod.local
MAIL_FROM_NAME="Registros Financeiros - Prod"

# ── Logs ──────────────────────────────────────────────────────────────────────
LOG_CHANNEL=stderr
LOG_LEVEL=warning

# ── Tag da imagem ─────────────────────────────────────────────────────────────
# IMAGE_TAG configurada pelo GitHub Actions a cada deploy (sha-<7-char>).
# Para primeiro deploy manual (antes do pipeline existir), deixe
# esta linha comentada — o up.sh fará build local com tag latest.
#IMAGE_TAG=sha-PLACEHOLDER
